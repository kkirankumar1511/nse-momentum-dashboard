[Unit]
Description=NSE daily rebalance scan (proposal only by default; also executes as real orders if auto_execute_trades is enabled in Admin -- Strategy configuration)

[Service]
Type=oneshot
User=nseapp
WorkingDirectory=/opt/nse-momentum-dashboard
ExecStart=/opt/nse-momentum-dashboard/.venv/bin/python live_rebalance.py
